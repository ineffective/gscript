#!/bin/bash

PROF_OPTS=
USE_JAR=${USE_JAR:-/home/jd/gs/target/gamescript.jar}
if [ "$useProfiler" = 1 ]
then
	PROF_OPTS=-agentlib:hprof=file=hprof.txt,cpu=samples,depth=5
fi

java $PROF_OPTS -cp $(cygpath -w ${USE_JAR}) com.gs.Script "${@}"

